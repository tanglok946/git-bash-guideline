git init (for first upload only)

git add .

git commit -m "inital commit"

git status

git branch -M main

git remote add origin http....

git push -u origin main

#note that this only applicable to first commit only (repo is empty)

